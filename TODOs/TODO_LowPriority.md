# Low Priority

- [ ] **Add More Input Validation:** More specific validation should be added to all `create` and `update` rules in Firestore to improve data integrity.
- [ ] **Update Outdated Dependencies:** Some npm packages (e.g., `react-router`) are outdated. These should be updated to their latest versions.
