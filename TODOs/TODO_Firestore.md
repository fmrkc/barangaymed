# Firestore

- [ ] **Restrict `barangayId` Updates:** Users should not be able to change their own `barangayId` after it has been set. This should be enforced in the `firestore.rules` to prevent users from accessing data from other barangays.
